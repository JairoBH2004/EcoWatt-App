buildscript {
    ext {
        buildToolsVersion = "36.0.0"
        minSdkVersion = 24
        compileSdkVersion = 36
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
    }
    repositories {
        google()
        mavenCentral()
        maven { url 'https://www.jitpack.io' } // opcional
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.6.1") // 🔹 Más rápido y reciente
        classpath("com.facebook.react:react-native-gradle-plugin:0.75.1")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:2.1.20")
        // ❌ No pongas expo.modules aquí
    }
}

apply plugin: "com.facebook.react.rootproject"

// ✅ Aplica plugin desde node_modules
// apply from: "../node_modules/expo/modules.gradle"

// ⚙️ Optimización: compilación incremental
tasks.withType(JavaCompile) {
    options.incremental = true
}
